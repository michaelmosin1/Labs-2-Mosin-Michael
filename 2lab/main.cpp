#include "matrix.hpp"


int main(){
    mm::matrix mat(3, 3);
    mat.mm::matrix::fill_with_data();
    mat.print();
    //mm::matrix a(3,3);
    mm::matrix a = mat;
    a.print();

    mm::matrix mas[7];
    return 0;
}